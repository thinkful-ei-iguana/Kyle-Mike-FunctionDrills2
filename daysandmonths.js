function daysAndMonths(month, ){
    var days;
    if (month !== 'January', 'February', 'March', 'April', 'May', 'June', 'July'
    switch(month){
       case 'April':
        case 'June':
        case 'September':
        case 'November':
        days = `${month} has 30 days.`;
        case 'February':
            if ()
        break;
        default :
        days = `${month} has 31 days`;
        }
        return days;
        
   }
console.log(daysAndMonths('kyle'));